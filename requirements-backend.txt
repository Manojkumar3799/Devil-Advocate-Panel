# Backend dependencies — everything the FastAPI process needs.
# Excludes streamlit (frontend-only).

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# AI / LangGraph stack (same as current root requirements.txt)
langgraph>=0.4
langchain>=0.3
langchain-core>=0.3
langchain-xai>=0.2
langchain-google-genai>=2.1
langchain-groq>=0.3
langchain-tavily>=0.1

# Database
supabase>=2.0
vecs>=0.4

# PDF export
reportlab>=4.0

# Utilities
python-dotenv>=1.0
httpx>=0.27
